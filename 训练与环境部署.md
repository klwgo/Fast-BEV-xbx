## 训练与环境部署

**训练准备**

- 保证 data/nuscenes 下是完整 v1.0-trainval 结构（含 v1.0-trainval、maps、maps_bev_seg_gt_2class 等），可通过 ls data/nuscenes 自检。
- 若尚未生成 4D 序列标注，运行：

```bash
python tools/create_data.py nuscenes \
  --root-path data/nuscenes \
  --out-dir data/nuscenes \
  --extra-tag nuscenes \
  --version v1.0 \
  --max-sweeps 10
python tools/data_converter/nuscenes_seq_converter.py

```

生成的 nuscenes_infos_{train,val}_4d_interval3_max60.pkl 将被配置直接读取。

**训练命令**

- 分布式训练（参考论文配置）：

```bash
LOG_DIR=logs
mkdir -p $LOG_DIR
LOG_FILE=${LOG_DIR}/fastbev_train_$(date +%Y%m%d_%H%M%S).out

nohup bash -lc '
  source /Data/xvboxun/anaconda3/etc/profile.d/conda.sh
  conda activate fastbev

  export DATAROOT=data/nuscenes
  export CFG=configs/fastbev/exp/paper/fastbev_m4_r50_s320x880_v250x250x6_c256_d6_f4.py
  export WORK=work_dirs/fastbev/exp/paper/fastbev_m4_r50_s320x880_v250x250x6_c256_d6_f4_dual4090

  CUDA_VISIBLE_DEVICES=6,7 \
  python -m torch.distributed.launch --nproc_per_node=2 tools/train.py $CFG \
    --launcher pytorch \
    --work-dir $WORK \
    --cfg-options \
      data.samples_per_gpu=3 \
      data.workers_per_gpu=6 \
      data.train.dataset.data_root=$DATAROOT \
      data.val.data_root=$DATAROOT \
      data.test.data_root=$DATAROOT \
      data.train.dataset.version=v1.0-trainval \
      data.val.version=v1.0-trainval \
      data.test.version=v1.0-test \
      data.train.dataset.ann_file=$DATAROOT/nuscenes_infos_train_4d_interval3_max60.pkl \
      data.val.ann_file=$DATAROOT/nuscenes_infos_val_4d_interval3_max60.pkl \
      data.test.ann_file=$DATAROOT/nuscenes_infos_val_4d_interval3_max60.pkl \
      optimizer.lr=0.0008 \
      lr_config.warmup_iters=2000 \
    --resume-from $WORK/latest.pth
' > "$LOG_FILE" 2>&1 &

# 查看进度
tail -f "$LOG_FILE"
```

- 继续训练命令

```bash
CUDA_VISIBLE_DEVICES=6,7 \
python -m torch.distributed.launch --nproc_per_node=2 tools/train.py $CFG \
  --launcher pytorch \
  --work-dir $WORK \
  --resume-from $WORK/latest.pth \
  --cfg-options \
    data.samples_per_gpu=2 \
    data.workers_per_gpu=6 \
    data.train.dataset.data_root=$DATAROOT \
    data.val.data_root=$DATAROOT \
    data.test.data_root=$DATAROOT \
    data.train.dataset.version=v1.0-trainval \
    data.val.version=v1.0-trainval \
    data.test.version=v1.0-test \
    data.train.dataset.ann_file=$DATAROOT/nuscenes_infos_train_4d_interval3_max60.pkl \
    data.val.ann_file=$DATAROOT/nuscenes_infos_val_4d_interval3_max60.pkl \
    data.test.ann_file=$DATAROOT/nuscenes_infos_val_4d_interval3_max60.pkl \
    optimizer.lr=0.0008 \
    lr_config.warmup_iters=2000
```

- 若只想从配置内指定的 load_from 继续，也可以不加 --resume-from，但断点续训时务必保持 $WORK 路径、samples_per_gpu 等设置与之前一致。

**评估与验证**

- 训练中

   evaluation.interval=5

  ，第 5/10/15/20 epoch 会自动在 val 上评估；最终可手动测试：

  `python tools/test.py $CFG $WORK/epoch_20.pth \  --eval bbox \  --cfg-options \    data.val.data_root=$DATAROOT \    data.val.ann_file=$DATAROOT/nuscenes_infos_val_4d_interval3_max60.pkl `

- 按论文设置跑满 20 个 epoch，期望 NDS≈0.41、mAP≈0.30（m4 配置）。

**后续**

1. 观察 GPU/显存占用，如需降显存可调 samples_per_gpu 或启用 with_cp=True。
2. 训练完成后保存 work_dirs，便于阶段化微调或继续进行后续多任务扩展。

## 环境

```bash
if [ -n "$CONDA_PREFIX" ]; then
  export CUDA_HOME=$CONDA_PREFIX
  export PATH=$CUDA_HOME/bin:$CONDA_PREFIX/bin:$PATH
  export LD_LIBRARY_PATH=$CUDA_HOME/lib:$CUDA_HOME/lib64:$LD_LIBRARY_PATH
  export CPATH=$CUDA_HOME/include:$CPATH
  export CC=$CONDA_PREFIX/bin/x86_64-conda-linux-gnu-gcc
  export CXX=$CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++
fi
```

**1. 在 Conda 环境里安装新版编译器**

```bash
conda activate fastbev
conda install -c conda-forge gxx_linux-64=9.5.0 ninja -y
```

**2. 重定向编译器到 Conda 提供的版本**

安装完成后，设置环境变量让 setup.py 使用新 g++：

```bash
conda activate fastbev
export CC=$CONDA_PREFIX/bin/x86_64-conda-linux-gnu-gcc
export CXX=$CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++
export PATH=$CONDA_PREFIX/bin:$PATH
hash -r
which g++
g++ --version
```

3、编译指令

```bash
CC=$CONDA_PREFIX/bin/x86_64-conda-linux-gnu-gcc \
CXX=$CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++ \
MMCV_WITH_OPS=1 python -m pip install -v -e . 2>&1 | tee build.log
```

4、找不到crypt.h

```bash
conda install -c conda-forge libxcrypt -y
```

5、**使用 Conda 提供的 CUDA 11.3 toolchain**

```bash
conda activate fastbev
conda install -c conda-forge cuda-nvcc=11.3  cuda-cudart-dev=11.3 cuda-cudart=11.3 -y
# 若提示找不到某个包，可加上 -c nvidia --override-channels
export CUDA_HOME=$CONDA_PREFIX
export PATH=$CUDA_HOME/bin:$PATH
export LD_LIBRARY_PATH=$CUDA_HOME/lib:$CUDA_HOME/lib64:$LD_LIBRARY_PATH
hash -r
which nvcc        # 应指向 $CONDA_PREFIX/bin/nvcc
nvcc --version    # 显示 CUDA 11.3

```

安装匹配 PyTorch 1.10 的 CUDA 11.3 toolchain（含 nvcc）：

```bash
conda install -c nvidia -c conda-forge --override-channels \
  cudatoolkit=11.3 cuda-nvcc=11.3 -y

```

设置环境变量让编译器指向这套 CUDA：

```bash
export CUDA_HOME=$CONDA_PREFIX
export PATH=$CUDA_HOME/bin:$PATH
export LD_LIBRARY_PATH=$CUDA_HOME/lib:$CUDA_HOME/lib64:$LD_LIBRARY_PATH
hash -r
which nvcc         # 应该输出 $CONDA_PREFIX/bin/nvcc
nvcc --version     # 确认版本是 11.3

```

确认

```bash
cd /Data/xvboxun/xbx/Fast-BEV-dev   # 换成你的实际仓库路径
rm -rf build mmdet3d.egg-info ~/.cache/torch_extensions/*
export PYTHONPATH=$(pwd):$PYTHONPATH
CC=$CONDA_PREFIX/bin/x86_64-conda-linux-gnu-gcc \
CXX=$CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++ \
MMCV_WITH_OPS=1 python -m pip install -v -e .
```

编译阶段提示 cuda_runtime.h 缺失，说明当前 $CUDA_HOME/include/ 里根本没有该头文件（conda 的 cuda-nvcc 只带了编译器，不含 runtime 头文件）。处理方式：

1. **先确认 include 是否缺失**

   ```bash
   conda activate fastbev
   echo $CUDA_HOME        # 按之前设置应等于 $CONDA_PREFIX
   ls $CUDA_HOME/include/cuda_runtime.h
   ```

2. **补齐 11.3 的完整 toolkit（含头文件）**
   可以直接从 NVIDIA 官方 channel 装整套 toolkit，并覆盖其它 channel：

```bash
conda install --override-channels -c nvidia -c conda-forge \
    cuda-toolkit=11.3.0 -y
```

3、**重新设置环境变量**

```bash
export CUDA_HOME=$CONDA_PREFIX
export PATH=$CUDA_HOME/bin:$PATH
export LD_LIBRARY_PATH=$CUDA_HOME/lib:$CUDA_HOME/lib64:$LD_LIBRARY_PATH
export CPATH=$CUDA_HOME/include:$CPATH
hash -r
which nvcc # 再次确认仍指向 $CONDA_PREFIX/bin/nvcc
nvcc --version # 保持 11.3
ls $CUDA_HOME/include/cuda_runtime.h # 现在应能看到文件
```

4、**清理旧缓存并重新安装** 

```bash
cd /Data/xvboxun/xbx/Fast-BEV-dev   # 换成实际仓库路径
rm -rf build mmdet3d.egg-info ~/.cache/torch_extensions/*
export PYTHONPATH=$(pwd):$PYTHONPATH
CC=$CONDA_PREFIX/bin/x86_64-conda-linux-gnu-gcc \
CXX=$CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++ \
MMCV_WITH_OPS=1 python -m pip install -v -e .
```

验证

```bash
python - <<'PY'
import torch, mmcv, mmdet3d
from mmdet3d.ops import furthest_point_sample_ext
print("torch:", torch.__version__)
print("mmcv:", mmcv.__version__)
print("mmdet3d:", mmdet3d.__version__)
PY
```
