没问题！下面把**环境已就绪后**的一整套常用运行命令浓缩成“速查表”，并把你的配置路径改为
 `configs/fastbev/exp/paper/fastbev_m4_r50_s320x880_v250x250x6_c256_d6_f4.py`。

------

# 0）变量（建议先设好）

```bash
# 按你的目录实际情况修改
export DATAROOT=data/nuscenes
export CFG=configs/fastbev/exp/paper/fastbev_m4_r50_s320x880_v250x250x6_c256_d6_f4.py
export CKPT=work_dirs/fastbev/exp/paper/fastbev_m4_r50_s320x880_v250x250x6_c256_d6_f4/epoch_20.pth
export RES=runs/fastbev_mini.pkl
export SHOWDIR=runs/vis_fastbev_mini_offline
```

> 备注：你之前已经生成好 `4D` 标注 pkl（如 `nuscenes_infos_val_4d_interval3_max60.pkl`），**无需再生成**。

------

# 1）离线推理（保存结果，不弹窗）

```bash
python tools/test.py $CFG $CKPT \
  --eval bbox \
  --out $RES \
  --cfg-options \
    data.val.data_root=$DATAROOT \
    data.val.ann_file="$DATAROOT/nuscenes_infos_val_4d_interval3_max60.pkl" \
    data.test.data_root=$DATAROOT \
    data.test.ann_file="$DATAROOT/nuscenes_infos_val_4d_interval3_max60.pkl" \
    data.samples_per_gpu=1 data.workers_per_gpu=2
```

------

# 2）离线可视化（把结果渲染为图片/每样例一个文件夹）

```bash
python tools/misc/visualize_results.py $CFG \
  --result $RES \
  --show-dir $SHOWDIR
```

> 如果你按我之前的建议做了“小幅补丁”（自适应相机数 / 安全写视频），这里会同时生成每个样例的图片与（可选的）视频；否则这一步至少会产出 **81 个样例文件夹**与 PNG。

------

# 3）一键汇总视频（按你定制的“三行布局”：上=前/前左/前右，中=后/后左/后右，下=BEV）

> 这是我们前面写的汇总脚本（已保存到 `tools/misc/make_all_video_gt_pred_3rows_dedupe.py` 后）：

```bash
# 可选安装：imageio-ffmpeg（装不上会自动回退到 OpenCV 写视频）
pip install -U imageio imageio-ffmpeg

python tools/misc/make_all_video_gt_pred_3rows_dedupe.py \
  --root   $SHOWDIR \
  --out-gt runs/all_folders_gt_3rows.mp4 \
  --out-pred runs/all_folders_pred_3rows.mp4 \
  --fps 8 --dedupe --dedupe-thr 1.5
```

- 会遍历 `$SHOWDIR` 下 **81 个样例文件夹**；
- 每个样例取 **4 个时刻×6张相机**（按你给的排序重排），并与 **`bev_gt.png` / `bev_pred.png`** 上下拼接；
- 生成两个总视频：`all_folders_gt_3rows.mp4` 与 `all_folders_pred_3rows.mp4`；
- `--dedupe` 可去除“连续近似重复帧”（阈值可调）。

------

\##（可选）单个样例快速出“上 6 图 + 下 BEV”的 demo

```bash
python tools/misc/make_demo_6cams_over_bev_by_indices.py \
  --dir $SHOWDIR/<某个样例文件夹> \
  --out runs/demo_6cams_over_bev.mp4 \
  --fps 6
```

------

### 小贴士

- 如果需要固定单卡运行，可在 `tools/test.py` 前加：`CUDA_VISIBLE_DEVICES=0`。
- 若 `CKPT` 路径不同，改成你实际的权重文件即可。
- 如果你之后切到 **m6（6相机）** 配置，命令不变，只改 `$CFG/$CKPT`。